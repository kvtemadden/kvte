import Layout from "@/app/layout";
import { Eyes } from "@/components/eyes";

const Page = () => {
  return (
    <Layout>
      <main className="pt-40">
        <Eyes />
      </main>
    </Layout>
  );
};

export default Page;
